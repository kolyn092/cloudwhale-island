# 구현 기준

게임 규칙은 상태 계층에 둔다. 화면·입력은 규칙을 복제하거나 자원과 시설 단계를 직접 바꾸면 안 되며 공개 행동을 호출한다.

시계 변경, 최대 오프라인 시간, 불완전한 생산 주기, 손상 저장, 저장소 실패는 자동 검사로 고정한다.

새 시설·자원·저장 필드는 상태 모델, 직렬화·검증, 저장 복구, 화면 표시, 자동 검사를 함께 갱신한다. Unity의 meta 파일은 해당 에셋과 함께 커밋하고 Library, Builds, Logs, 임시 테스트 결과는 커밋하지 않는다.
