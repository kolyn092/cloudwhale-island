# 데이터와 비밀 정보

진행 데이터는 현재 브라우저 로컬 저장소에만 있다. 로그인·계정·서버 검증이 없으므로 저장 문자열은 신뢰할 수 없는 입력이며 형식, 음수 자원, 집 단계와 정원 시설 목록을 검사해야 한다.

유효하지 않은 데이터는 일부 값을 추측해 살리지 않고 새 게임으로 복구한다. 저장소 오류는 숨기지 않으며 플레이어에게 비저장 상태를 알려야 한다.

정적 Web 빌드에는 비밀 키, 외부 AI 키, 계정 토큰을 넣지 않는다.
